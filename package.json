{
  "name": "skyfire-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SkyFire 天火遠征：手機與桌面可玩的原創縱向彈幕射擊 MVP（對標 Thunderfall，CC0-1.0 程式碼級複用）",
  "scripts": {
    "start": "node serve.mjs",
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
