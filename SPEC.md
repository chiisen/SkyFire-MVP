# SkyFire-MVP 規格書（對標 Thunderfall 1:1）

- 狀態：草稿（Phase 4 送審中）
- 對標來源：https://github.com/MartinDelophy/awesome-gpt-6-astra/tree/main/works/thunderfall（`README.md` / `DESIGN.md` / `PROMPT.md` / `metadata.json`，讀取日期 2026-09-11）
- 技術路線：原生 Web（HTML + CSS + JavaScript ES Modules + Canvas 2D + Web Audio），零第三方運行依賴
- 授權：Thunderfall 倉庫為 CC0-1.0，原創程式碼、程序畫面與合成聲音可複用；機制研究參考 MOSS《雷電 IV》與 CAVE《怒首領蜂最大往生》官方系統說明，不使用其程式碼、美術、音訊或商標

## 1. 背景與目標（Why）

- **Who**：SkyFire-MVP 開發者（單人）與後續玩家（手機豎屏 + 桌面瀏覽器）。
- **現況（已驗證）**：本倉庫目前僅有 `README.md`（2 行），無遊戲程式碼、無測試、無建置流程。
- **目標行為**：建立一份與 Thunderfall 同等完整度的規格與實作基線：3 種戰機、4 色武器、5 關戰役（至少 600 秒有效戰鬥）、首領三階段、關間三選一成長、標準 / 街機雙模式。
- **Why now**：以已被驗證的設計為起點，避免從零摸索彈幕公平性、戰役節奏與手機操作。
- **完成定義**：`SPEC.md` 落倉庫根目錄；實作滿足 §8 驗收標準；`npm test` 全綠；靜態部署可匿名遊玩。

## 2. 資源複用策略（使用者硬約束）

> 關鍵事實：Thunderfall `works/thunderfall/` 內**沒有** `.png` / `.jpg` / `.mp3` / `.wav` / `.ogg` 圖檔音檔。
> `audio.js` 首行即註明全本地合成、無音檔請求；畫面由程式繪製；唯一圖形檔是 `icon.svg`。
> 因此「務必使用其美術圖與音效音樂」只能落實為**程式碼級複用**，而非二進位檔拷貝。

| Thunderfall 來源                       | 內容                                                                                            | SkyFire 複用方式                                 |
| -------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `airframes.js`                         | 三架戰機程序三維網格（紅白後掠翼 / 藍銀前掠翼 / 金色重裝）、九種側傾姿態快取                    | 整檔移植，允許改命名與色碼，視覺受擊分離規則不變 |
| `render.js`                            | Canvas 2D 投影、地形、敵機、彈幕、雷射預警繪製；敵彈最後繪製                                    | 整檔移植；粒子與殘影數量上限保留（手機效能）     |
| `audio.js`                             | Web Audio 合成：射擊 / 擊殺 / 拾取 / 受擊 / 炸彈 / 首領 / 爆發 / 勝敗 + `tick()` 背景音樂步進器 | 整檔移植；靜音開關行為不變                       |
| `icon.svg`                             | 作品圖示                                                                                        | 直接複用或改色後使用                             |
| `assets/screenshots/thunderfall/*.jpg` | 僅為展示截圖                                                                                    | **不可**當作遊戲內素材內嵌，僅作美術對照參考     |
| `data.js` / `engine.js` / `app.js`     | 數值、引擎、頁面控制                                                                            | 邏輯 1:1 移植，見 §6                             |

- 複用保留 CC0-1.0 聲明，出處以本規格書本節清單為準（原創幾何與合成聲音）；README 不另行註明。
- 任何新增美術/音效亦須以程序生成為原則，不引入外部二進位資產；若將來引入，須更新本節清單。

## 3. 技術約束

- 原生 JavaScript（ES Modules），Canvas 2D，Web Audio；無框架、無外部字體/圖片/音檔、無帳號/API Key。
- 邏輯畫布 480×800；手機與桌面共用同一碰撞邏輯與戰鬥計時，不隨實體像素改變。
- 須可經 `file://` 以外之 HTTP 方式遊玩；建置與測試僅用 Node.js 內建模組。
- 本機運行：`python3 -m http.server 4173` 後開 `http://localhost:4173`；測試：`npm test`；建置：`npm run build`（鏡像運行檔至 `dist/` 並 minify，靜態託管即可發布）。

## 4. 玩法規格

### 4.1 操作

- 全程自動開火；移動：桌面 WASD/方向鍵/滑鼠拖動，手機單指相對拖動（按下不瞬移，手指可置於機體下方）。
- 精密低速：Shift 或「精密移動」按鈕；炸彈：空白鍵或按鈕；爆發：E 或按鈕；暫停：P/Esc/按鈕。
- 技能按鈕 ≥ 44×44 CSS px，支援雙指並行（移動 + 施放）；失焦/切後台自動暫停，恢復須玩家確認，不補算離線時間；暫停凍結補給壽命、彈幕與技能時鐘。

### 4.2 三種戰機

| 戰機      | 機體/護盾 | 移速 | 傷害/射速 | 炸彈 | 定位               |
| --------- | --------- | ---- | --------- | ---- | ------------------ |
| 鷹隼 F-01 | 5/2       | 330  | 1.00/1.00 | 2    | 均衡靈活           |
| 稜鏡 P-02 | 4/2       | 300  | 1.08/1.00 | 2    | 高傷薄甲，雷射起步 |
| 堡壘 B-03 | 7/3       | 260  | 0.90/0.95 | 3    | 重裝生存           |

- 中央獨立受擊核心（小光點），機翼視覺範圍 ≠ 受擊範圍；受擊後短暫無敵。
- 實戰隨橫向輸入側傾；每架最多快取九種戰鬥姿態；爆發時尾焰增強；升級出現僚機。

### 4.3 五關戰役

- 順序：晨曦海港 → 翡翠峽谷 → 冰川陣列 → 熔核工廠 → 天穹核心。
- 每關至少 120 秒**有效戰鬥時間**；第 88 秒出首領；入場 2 秒無敵；機體 65% / 30% 各觸發 1.15 秒階段屏障（清彈停火，越界單發傷害停在閾值，不可跳階段）。
- 首領擊破後續獎勵波 + 至少 10 秒拾取窗口；三條件全滿才過關：首領已死、計時達 120 秒、拾取窗口結束。逾時未殺則續戰至擊破。
- 完整通關 ≥ 600 秒（10 分鐘）有效戰鬥；選機/暫停/關間/結算不計時。
- 首領機體：6000 / 10500 / 16500 / 22000 / 28000；街機模式 +15%。
- 攔截機波次左右交替進場，不可單側連發。

### 4.4 敵人與彈幕

- 敵人：斥候、左右交替攔截機、炮艇、雷射機、運輸機、佈雷單位。
- 彈幕：瞄準彈、彎曲漂移彈（連續軌跡可讀）、旋轉彈陣、加速彈、分裂彈；雷射分預警/生效兩段，預警零傷害且視覺明確；第四關加入掃動光束。
- 保證彈幕間隙可通過；漂移彈不瞬移跳躍。

### 4.5 武器掉落與成長

- 四色武器：藍 P 風暴脈衝（扇形散射，掉落後條件機率 48%）/ 綠 L 翡翠光束（穿透 28%）/ 紫 A 紫電追獵（追蹤 18%）/ 金 N 日冕新星（範圍爆裂 6%）。上表為**已掉落後**的條件機率，非每次擊殺率。
- 擊殺概率掉落；運輸機 + 連續未掉落保底補正；補給於可玩區內漂移反彈，約 10 秒有效戰鬥時間後消失，消失前閃爍；暫停凍結壽命；字母+外形+顏色三重區分。
- 同款拾取升級至 Lv.5；切換武器保留各款等級；另有修復/護盾/炸彈補給。
- 關間三選一：傷害（疊加 +15%/次）、射速（+12%/次）、機體（上限+1並修 2）、護盾（上限+1並補滿）、吸附範圍、僚機（最多 2 架，滿則不再出現）、炸彈（庫存上限 5）、充能（擦彈/擊殺充能 +30%）。
- 固定過關補給：修 2 點機體、炸彈 +1、下一關護盾補滿；卡片收益另行疊加。
- 連擊最高 ×5；擦彈得分並充能；震盪炸彈清彈傷敵+短暫無敵；滿充能可啟 8 秒雷霆爆發（啟動清彈，期間新彈仍須躲）。

### 4.6 模式與續戰

- 標準模式：機體剩 1、護盾耗盡且有炸彈時自動觸發炸彈保護；街機模式：彈速與首領耐久提升，須手動炸彈。
- 單關演練：任選一關，不計入戰役最高分。
- 續戰上限 3 次：重開當前關、保留強化、分數扣 35%、已用戰鬥時間保留。
- 持續避彈 13 秒開始回護盾，須繼續保持安全才回更多。

## 5. 畫面、聲音、HUD

- 機庫斜視 + 戰鬥 HUD（街機氣質、克制裝飾）；手機窄豎屏完整顯示戰場 + HUD；裝飾動態可關；效果強度不遮擋敵彈。
- 聲音由使用者手勢後啟動；靜音不影響玩法；最高分與偏好存當前瀏覽器本地。

## 6. 檔案結構（對標移植）

| 檔案               | 變更                                                                         |
| ------------------ | ---------------------------------------------------------------------------- |
| `index.html`       | 入口，手機 viewport + 按鈕佈局                                               |
| `src/style.css`    | HUD/機庫/按鈕樣式                                                            |
| `src/data.js`      | 戰機/武器/關卡/首領數值（§4）                                                |
| `src/engine.js`    | 主迴圈、固定步長、碰撞、掉落、計時、暫停凍結                                 |
| `src/render.js`    | 繪製筒倉：轉出口＋組幀（§2）                                                 |
| `src/render/`      | 子模組：`shared` 圖元／`background` 背景／`units` 單位／`combat` 戰鬥物件    |
| `src/airframes.js` | 自 Thunderfall 移植（§2）                                                    |
| `src/audio.js`     | 自 Thunderfall 移植（§2）                                                    |
| `src/app.js`       | 頁面進入點：建立狀態、接線、開主迴圈                                         |
| `src/app/`         | 子模組：`state` 狀態／`hud` 介面／`screens` 流程／`input` 接線／`frame` 迴圈 |
| `build.mjs`        | 零依賴建置，輸出 `dist/`（鏡像原始碼佈局＋minify）                           |
| `tests/`           | 引擎單元測試（§8）                                                           |
| `icon.svg`         | 自 Thunderfall 複用                                                          |

## 7. 範圍外

- 多人連線、帳號系統、後端排行榜、課金、外部素材市集。
- 一次寫死固定擊殺秒數（實際耗時取決於武器/命中/技能，見 DESIGN 對應說明）。

## 8. 驗收標準（可測、非感覺）

1. 三戰機皆可開局，數值如 §4.2。
2. 五關推進不跳過最低戰鬥計時；首領三階段完整展示；600 秒下限以固定步長引擎測試驗證。
3. 雷射預警零傷害；漂移彈軌跡連續；補給 10 秒壽命隨暫停凍結。
4. 四武器可裝備、可升至 Lv.5、切換保留等級。
5. 三選一升級生效；僚機/炸彈上限遵守；保底掉落可觀測。
6. 觸控移動時可另一指操作炸彈；按鈕 ≥ 44×44 CSS px。
7. `npm test` 全綠（對標 27 項：戰機/武器/補給/邊界反彈/暫停凍結/雷射預警/漂移/掃掠/升級/續戰/首領流程）。
8. 自動駕駛完整戰役模擬可通關（流程與平衡檢查，非真人難度證明）。
9. 真機聲明誠實標示：引擎模擬 ≠ 真人 10 分鐘通關；未做實物手機測試不得宣稱已做。

## 9. 測試金字塔

| 層   | 內容                                                                                    | 數量指引       |
| ---- | --------------------------------------------------------------------------------------- | -------------- |
| 單元 | 引擎純邏輯（計時、掉落、升級、屏障、續戰）                                              | ≥ 27           |
| 模擬 | 固定步長全戰役 autopilot                                                                | ≥ 1 次完整通關 |
| 手動 | 瀏覽器試玩（選機/拖動/拾取/炸彈/暫停/結算）+ 多解析度（390×844、375×667、667×375 橫屏） | 每版 1 輪      |
| 實機 | 至少一台真實手機試玩與效能記錄                                                          | 發布前 1 次    |

## 10. 部署

- 靜態託管即可；若用 Vercel：Root Directory 為本倉庫根目錄，Framework Other，Build `npm run build`，Output `dist`，免環境變數。

## 11. DoD

- 範圍符合本規格；`npm test` 綠；`git diff --check` 通過；更新 CHANGELOG；複用聲明完整。
